---
description: Turn a long research response (or an existing document) into a reviewable markdown artefact with inline AI threads in a local browser UI
---

The user wants to start an inline discussion.

Invoke the `inline-discussion:discuss` skill. The skill's SKILL.md has the full
flow — read it and follow it.
