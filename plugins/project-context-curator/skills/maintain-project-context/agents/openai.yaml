interface:
  display_name: "Maintain Project Context"
  short_description: "Capture durable project and domain context."
  default_prompt: "Use $maintain-project-context to capture durable context and, when setting up a domain, ask whether membership is explicit or root-based."
